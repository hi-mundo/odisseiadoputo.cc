#!/bin/sh
# Falha o commit se o site não compilar (alinhado ao build do Cloudflare Pages).
set -e
cd "$(git rev-parse --show-toplevel)"
if ! command -v bundle >/dev/null 2>&1; then
  echo "pre-commit: 'bundle' não encontrado no PATH." >&2
  exit 1
fi
exec bundle exec jekyll build
